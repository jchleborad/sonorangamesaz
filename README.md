# Sonoran Games

Phone-first static frontend for the private Sonoran Games NFL Pick'em platform.

## Local preview

From the repository root:

```bash
python -m http.server 8080
```

Then open `http://localhost:8080`.

The Home page and Live Scoreboard currently use representative prototype data. Apps Script integration will follow visual approval.
